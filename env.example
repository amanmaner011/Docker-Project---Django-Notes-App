# Copy this file to .env and adjust values as needed.
# .env is gitignored — never commit real credentials.

# MySQL connection details consumed by notesapp/settings.py
DB_NAME=test_db
DB_USER=root
DB_PASSWORD=root
DB_HOST=db
DB_PORT=3306
